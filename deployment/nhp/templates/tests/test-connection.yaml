apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nhp.fullname" . }}-test-connection"
  labels:
    {{- include "nhp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nhp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
